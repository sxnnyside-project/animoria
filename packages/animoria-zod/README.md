# @animoria/zod

Runtime schema validation for Animoria configurations, protocol envelopes, and domain objects powered by Zod.

## Features
- Validates `.animoriarc.json` configuration files
- Validates Daemon Protocol v1 incoming and outgoing messages
- Provides runtime type guards and parsing for untrusted inputs
