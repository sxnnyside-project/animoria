## Rules you opt into

No rule runs until `.animoriarc` says it should. An empty or absent config
means Animoria discovers and previews assets but enforces nothing — the
Health Score reports "not available" rather than a number, because no
policy exists yet to score against.
