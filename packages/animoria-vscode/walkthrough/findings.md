## Confidence is earned, not asserted

A finding's confidence reflects the evidence behind it: a byte-identical
duplicate is reported as certain, because byte equality is not a matter of
degree. An unreferenced-asset finding is capped by how much of the
workspace the reference scan actually covered — a search that skipped a
format cannot support a "certain" verdict.
