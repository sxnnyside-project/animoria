## Discovery, not configuration

Animoria's sidebar populates the moment it finds an asset — no build step, no
manual scan, no project file to add. Add a `.json` that parses as Lottie, a
`.riv`, a `.gif`, and it appears automatically the next time the workspace
index settles.
