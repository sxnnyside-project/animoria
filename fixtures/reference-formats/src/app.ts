import hero from '../assets/by-code-import.json';
// A variable merely named after an asset is not a reference:
const fpVariableName = 42;
// fp-prose.json is mentioned in a comment only, never used.
export default hero;
