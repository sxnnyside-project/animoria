// A path-shaped string naming another root's asset.
// Animoria must NOT credit this as a reference: root-c cannot resolve it,
// and matching on filename alone is exactly the ambiguity V2 removes.
export const NOT_A_REFERENCE = '../../root-a/assets/unreferenced-a.json';
