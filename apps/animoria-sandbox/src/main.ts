// Tokens are imported here rather than by a component, because they live on
// `:root` and a Lit shadow root cannot define them for its host.
import '@animoria/ui/tokens.css';
import './components/sandbox-app.js';
